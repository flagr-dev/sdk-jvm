# Run the Gradle daemon itself on the provisioned JDK, not the system JDK.
# This avoids issues with Kotlin's IntelliJ runtime and Java 25+.
org.gradle.java.installations.auto-download=true
org.gradle.java.installations.auto-detect=true
